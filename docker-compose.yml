services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - next_cache:/app/.next

volumes:
  node_modules:
  next_cache:
    env_file:
      - .env.local
    environment:
      # Windows Docker Desktop 에서 핫리로드 활성화
      - WATCHPACK_POLLING=true
    restart: unless-stopped
